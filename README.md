# Socket_Communication_ClientApp

This project implements HTTP client interface. The client reads the msg from the browser’s QUERY_STRING, sends the request to server through socket, receives the JSON response, and outputs it back to the browser as an HTTP response. It allows simple commands like Hello, Status, Time, and All via URL.